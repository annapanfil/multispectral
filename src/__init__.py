"""Main module to take photos, preprocess them, run inference and postprocess. 

Main is only run on the drone.
Main_drone and main_ground should be used together. Then the drone is capturing the photos and sending them via wifi to the ground computer, which does the next steps."""